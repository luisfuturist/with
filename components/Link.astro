---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<a class:list={["hover:underline", className]} {...rest}>
  <slot />
</a>
